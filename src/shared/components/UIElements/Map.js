import React from "react";
import "./Map.css";

function Map({ className, style }) {
  return (
    <div className={`map ${className}`} style={style}>
      Map
    </div>
  );
}

export default Map;
